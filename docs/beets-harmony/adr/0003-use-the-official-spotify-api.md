# Use the official Spotify API

Spotify discovery will use the official Web API with one configured market and client credentials supplied through environment variables populated by agenix. This adds a Spotify Developer app and Premium account requirement, but avoids depending on web-player token extraction, scraping, or unauthenticated proxies whose behavior and availability are outside the supported Spotify contract.
